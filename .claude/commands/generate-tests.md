Review `pipelines/adjustments/factors.py` and create a robust pytest suite for split, bonus, dividend, and missing-date edge cases.

Do not rewrite production logic unless a minimal fix is required to make the tests valid.