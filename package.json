{
  "name": "tickertruth",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "TickerTruth — versioned NSE symbol lineage and corporate action reference data. Static marketing site deployed to Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "vitest run",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.62.1",
    "eslint": "^10.9.1",
    "globals": "^17.11.0",
    "vitest": "^4.1.11",
    "wrangler": "^4.125.0"
  }
}
